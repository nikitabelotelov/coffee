// @ts-ignore
import Deferred = require("Core/Deferred");
// @ts-ignore
import ITransport from 'Browser/_Transport/ITransport';
import Headers  from 'Browser/_Transport/RPC/Headers';

export type RequestParam = {
    data: string;
    headers: Headers;
    // @ts-ignore
    transport: ITransport;
    timeout?: number;
    url: string;
    method: string;
}

export type CallFunction = (params: RequestParam) => Deferred<object>
