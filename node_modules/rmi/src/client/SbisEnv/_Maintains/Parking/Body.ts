/// <amd-module name="SbisEnv/_Maintains/Parking/Body" />
// @ts-ignore
import * as Control from 'Core/Control';
import 'css!SbisEnv/_Maintains/Parking/style';
import * as _template from 'wml!SbisEnv/_Maintains/Parking/Body';
import Config from "SbisEnv/_Maintains/Parking/Config";
import { Size } from 'SbisEnv/_Maintains/Parking/Const';

let getSize = (options: Config) => {
    return options.size;
};

class Body extends Control {
    size: Size;
    _template = _template;
    constructor(options: Config) {
        super(options);
        this.size = getSize(options);
    }
}

export default Body;
