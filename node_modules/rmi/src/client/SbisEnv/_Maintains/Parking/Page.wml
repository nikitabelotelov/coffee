<div class="SbisEnv-ParkingPage">
    <SbisEnv._Maintains.Parking.Header
        attr:class="SbisEnv-ParkingPage__header"
        scope="{{ _options.header }}"
    />
    <SbisEnv._Maintains.Parking.Body:default
        image="{{ _options.image }}"
        message="{{ _options.message }}"
        details="{{ _options.details }}"
        action="{{ _options.action }}"
        size="large"
    />
</div>
