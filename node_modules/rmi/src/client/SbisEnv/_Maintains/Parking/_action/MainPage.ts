/// <amd-module name="SbisEnv/_Maintains/Parking/_action/MainPage" />
// @ts-ignore
import * as Control from 'Core/Control';
import * as _template from 'wml!SbisEnv/_Maintains/Parking/_action/link';
import { ACTION } from "SbisEnv/_Maintains/Parking/Const";

class MainPage extends Control {
    _template = _template;
    caption = rk(ACTION.MAIN_PAGE);
    action() {
        if (typeof window === 'undefined') {
            return;
        }
        // @ts-ignore
        window.location = '/';
    }
}

export default MainPage;
