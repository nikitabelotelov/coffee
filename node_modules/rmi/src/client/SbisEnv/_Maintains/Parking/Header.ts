/// <amd-module name="SbisEnv/_Maintains/Parking/Header" />
// @ts-ignore
import * as Control from 'Core/Control';
// @ts-ignore
import * as i18n from 'Core/i18n';
import * as _template from 'wml!SbisEnv/_Maintains/Parking/Header';
import 'css!SbisEnv/_Maintains/Parking/Header'
import { IMAGE } from './Const';

export default class Header extends Control {
   _template = _template;
   static getDefaultOptions() {
      return {
         // TODO: отдельное лого для каждой локализации
         logo: IMAGE.HEADER_LOGO, // i18n.getLang()
         title: 'Сеть деловых коммуникаций',
         locale: i18n.getLang(),
         lang: true,
      };
   }
}
