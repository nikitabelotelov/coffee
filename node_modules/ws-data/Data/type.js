//FIXME: temporarily because controls have depend on it
define('Data/type', ['Types/entity'], function(entity) {
   'use strict';
   return {
      Record: entity.Record,
      Model: entity.Model
   };
});
